FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci --only=production

# Copy build output
COPY dist ./dist

# Create data directory for sent jobs
RUN mkdir -p /app/data

EXPOSE 3001

# Start the API
CMD ["node", "dist/apps/api/main"]
